#include <iostream>
using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    double G, V, K;
    cin >> G;
    cin >> V;
    cin >> K;
    if (G = V) {
        cout << "Иди на хуй ";
    }
    else if (G = V) {
        cout << "Треугольник равнобедренный";
    }
    else if (V = K) {
        cout << "Треугольник равнобедренный";
    }
    else if (G = K) {
        cout << "Треугольник равнобедренный";
    }
    return 0;
}